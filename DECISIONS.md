# DECISIONS

每条决策注明状态。被推翻的决策标记 **Superseded by D-00N** 并说明原因，不删除——推理轨迹本身就是价值。

由于本项目在 2026-07-28 才引入治理文件，D-001..D-010 是从代码、README 与 webapp 文档中**回溯重建**的既有决策，依据见各自 Evidence 列。

| ID | 决策 | 理由 | 状态 | Evidence |
|---|---|---|---|---|
| D-001 | 双形态交付：Windows GUI + OpenWrt Docker Web | 桌面场景要完整控制；手机/局域网场景要随时看温度，两者共享同一套 iDRAC 凭据模型 | Active | E-005 |
| D-002 | 桌面版密码只保存在 GUI 进程内存，不写入任何项目文件 | 防止凭据随仓库或配置文件泄漏 | Active | E-006 |
| D-003 | 调用 `ipmitool -E`，密码经子进程环境变量传递 | 密码不出现在进程参数列表（`ps`/任务管理器可见）和 UI 日志里 | Active | E-007 |
| D-004 | 固定转速前必须解除安全联锁并成功关闭自动温控 | 防止误操作削弱服务器自我保护；“恢复自动温控”始终可用作兜底 | Active | E-008 |
| D-005 | Web 版无第二套 Web 密码，控制区直接用 iDRAC 凭据解锁 | 减少凭据数量；后端本地恒定时间比对，输错不消耗 iDRAC8 远程失败次数 | Active | E-009 |
| D-006 | Web 版 iDRAC 密码存宿主机 `secrets/idrac_password`，只读 Docker secret 挂载 | `.env` 易被误提交、镜像层会留存明文；secret 文件 `chmod 400` 且不复制进镜像 | Active | E-010 |
| D-007 | iDRAC 用 DHCP 时以网卡 MAC 为稳定身份；`IDRAC_MAC`、`IDRAC_DISCOVERY_CIDR` 缺失则容器拒绝启动 | 旧 IP 失效后需要可靠重新定位；宁可启动失败也不静默退回未核验的固定 IP | Active | E-011 |
| D-008 | 容器用独立 Docker 网络 + 独立默认拒绝防火墙区，固定容器 MAC，只放行 TCP 443 / UDP 623 / LAN→8080 | 把 `/24` 探测权限限定在单一容器身份，普通其他 Docker 容器无法冒用 | Active | E-012 |
| D-009 | 公开 Release 只提供源码和 Docker 包，不提供内嵌 `BMC.msi` 的 EXE | 未确认 Dell 再分发许可；源码构建者自行从合法来源取得签名一致的 MSI | Active | E-013 |
| D-010 | 开发在 WSL（代码、测试、Git），GUI 在 Windows Python 运行 | CustomTkinter GUI 需要直接调用 Dell Windows 版 `ipmitool.exe` | Active | E-014 |
| D-011 | Web 版接受局域网明文 HTTP | 公开遥测无凭据风险；控制区密码只在可信 LAN 内传输；跨不可信网络时要求前置 HTTPS 反代并设 `WEB_COOKIE_SECURE=true` | Active | E-015 |
| D-012 | 引入 ax3000t 式治理骨架（本套文件 + 证据纪律 + 每次改动即提交） | 项目此前只有一次 Git 提交，过程记忆为零，新会话无法接手 | Active | E-001, E-002 |
| D-013 | 桌面与 Web 是两条产品线，各自独立语义化版本，不强行统一。单一事实来源：桌面 = `pyproject.toml`（`r730xd_fan/__init__.py` 同步）；Web = `webapp/installer/VERSION`。Git tag 格式 `desktop-vX.Y.Z` / `web-vX.Y.Z`，发布说明必须注明产品线 | 两者发布节奏不同（Web 0.3.1 已发布时桌面已演进到 0.4.0），强行统一会制造无意义的版本跳跃；真正要消除的不是数字差异而是"哪个数字是真的"的不确定性 | Active | E-003, E-018 |
| D-014 | 两端前端统一为「近黑单色 + 颜色只表示报警」的设计语言：琥珀=warning、红=critical，界面其余部分不使用饱和色；三条趋势线用低饱和色区分。同时按第一性原理删除装饰性元素（背景网格与光晕、风扇仪表盘 SVG、快照条、卡片 sparkline、成排 eyebrow 标签、品牌方块、三处重复的连接状态） | 原冷蓝「blueprint」与其后的暖陶土配色都在给一个单用途运维工具附加与数据无关的性格；用户对两版都判断为一般。装饰色会和真正的报警色竞争注意力，而这是一个必须让异常一眼可见的工具。仪表盘/快照条是信息重复：前者只显示用户刚设的整数，后者被趋势图以更高分辨率覆盖 | Active | E-020 |
| D-015 | Web 版遥测历史落 SQLite（默认 `/data/telemetry.db`，独立 Docker named volume），保留期 30 天，写入批量刷盘，长区间在 SQL 里分桶降采样；`TELEMETRY_DB_PATH` 置空即回落到原内存模式，路径不可写时自动禁用持久化而不阻止启动 | 原 90 条内存样本重启即清零，无法回答「昨晚是不是热过」。宿主实测为 VMware 虚拟机（x86/64 Kwrt，`/proc/mtd` 为空，f2fs on VMware Virtual disk），不存在路由器裸 flash 磨损问题，落盘安全；仍做批量刷盘与保留期裁剪以控制体积与 IO。`read_only: true` 对 named volume 不生效，故容器安全约束无需放宽 | Active | E-021, E-022 |
| D-016 | 完整 SDR 扫描对匿名访客开放：`GET /api/sensors/deep-scan` 完全公开；`POST`（触发新扫描）也对匿名开放，但加全局冷却 `DEEP_SCAN_MIN_INTERVAL`（默认 60 s），冷却期内返回上次结果并带 `throttled: true`，已登录操作员不受冷却限制。所有写机器状态的接口边界不变 | SDR 是只读传感器遥测，与匿名仪表盘已公开的数据同类，没有凭据或拓扑泄漏。真正的风险不是数据而是负载：`sdr elist all` 对资源受限的 iDRAC8 是重操作（60 s 超时），完全放开会让局域网内任何人反复把 BMC 打满。因此拆成「读结果无限制、触发扫描有冷却」 | Active | E-024 |
| D-017 | 等宽字体只用于纯数字/纯拉丁读数（大数字、时间戳、图例数值）；任何可能包含中文的文本一律用正文字体，需要对齐时用 `font-variant-numeric: tabular-nums` | 中文在拉丁等宽字体里会回落到另一套 CJK 字形，导致同一行内出现两种字体，视觉上割裂——SDR 表头「名称/类型/读数/状态」和 `31.7 degrees C` 这类读数与圆体的传感器名并排时尤其明显 | Active | E-025 |
| D-018 | Redfish 链路用**证书指纹固定**（`REDFISH_TLS_FINGERPRINT`，SHA-256 叶证书）而不是 CA 校验；未配置时保持原行为 | iDRAC 是自签证书，打开普通 CA 校验做不到，但 `verify=False` + HTTP Basic 意味着 iDRAC root 密码会交给任何在 443 应答的东西——容器与 BMC 之间的中间人直接拿到最高权限凭据。指纹固定不需要任何 PKI，且 urllib3 在**握手阶段**即中断，`Authorization` 头不会被写出。默认不强制是因为线上 v0.3.1 尚未配置指纹，强制会直接打断现网 Redfish | Active | E-026 |
| D-019 | 登录限速的分桶键从单个 IP 改为 **/24 子网**（IPv6 为 /64） | D-005 让错误密码走本地恒定时间比对以保护 iDRAC 的失败次数配额，副作用是真实 BMC 账户**不会被锁定**，于是这个端点成了不会锁死的密码预言机，防线全压在限速上。而按精确地址分桶时，局域网内换一个 IP 就能重置预算——等于没有预算。代价：同一 /24 内的攻击者会连带锁住合法操作员，在家庭 LAN 场景下可接受 | Active | E-027 |
| D-020 | 「**公开路由永不改变硬件状态**」升级为可执行不变量，用行为测试锁定：匿名驱动每一个公开路由，断言没有任何 IPMI 写命令到达 BMC | 这条不变量此前只存在于代码阅读和 D-016 的叙述里，没有任何机制阻止将来有人把一个写操作挪到公开侧。安全模型的其余部分都建立在它之上 | Active | E-028 |
| D-021 | 内网可用即可上线：LAN 明文 HTTP（D-011）与未固定 Redfish 证书指纹（D-018）**接受为已知风险**，不作为发布阻塞项，转入待办（T-012 / T-013） | 该服务只在可信内网提供，业主判断本网段本身可信。**注意依据**：这两条的威胁模型从来不是外网暴露，而是同网段的被动嗅探与 ARP 欺骗——「没有外部访问」本身不构成风险消失的理由，成立的理由是「本网段可信」这个判断。二者不可混用，不得据此放宽其他边界 | Active | E-029 |
| D-022 | `ipmitool` 完整 SDR 遍历仅在进程以 `SIGSEGV`（直接返回 `-11` 或 shell 返回 `139`）退出、且 stdout 已解析出有效记录时，保留记录并明确返回 `partial=true` / `partial_reason=ipmitool_sigsegv`；其他非零退出与空结果仍按失败处理 | WRT 镜像内的 `ipmitool 1.8.19` 在 iDRAC8 2.70 上遍历 SDR 会于途中段错误，并可能暂时占用 BMC IPMI session。丢弃崩溃前的真实记录会诱使用户反复重试；但把任意非零退出都当部分成功又会掩盖认证或链路故障。该策略只恢复可用的部分数据，不宣称完整读取已修复 | Active | E-031 |
| D-023 | iDRAC 返回的 `PowerMetrics` 在「`PowerConsumedWatts > 0` 且 average/min/max 三者都存在且都为 0」时判定为无效，整组丢弃不下发；平均/最小/最大改由应用自己写入 SQLite 的样本计算，统计范围严格等于 `GET /api/telemetry/history` 本次返回的 samples，UI 必须写明「按当前趋势区间的图表样本计算」 | 一台正在抽 135 W 的机器不可能平均、最低、峰值同时恰好为 0 W，这不是测量值而是 iDRAC8 firmware 2.70 的缺陷（E-032）。把它显示成 0 W 会在实时 135 W 旁边给出一个自信的错误读数。整组丢弃而不是逐个猜哪个可信；`consumed == 0` 的真空载场景全零合理，保留。长区间 samples 已在 SQL 里 downsample，所以文案说「图表样本」而非逐点极值，避免制造假精度 | Active | E-032 |
| D-024 | 桌面版版本号从 0.4.0 直接跳到 **0.4.1**，与 Web 线当前版本相同；同时重新构建一体 EXE | D-014 的近黑单色重做是**一次同时改两条产品线**的改动，此后两端处于同一设计状态，业主判断此时让数字相同比各自演进更能反映事实。更要紧的是修一个真问题：`dist/` 里的 v0.4.0 EXE 构建于 2026-07-14，而 `ui.py` 最后改于 2026-08-15（E-036），产物里根本没有新 UI，版本号却完全相同——从外面无法分辨。**这不推翻 D-013**：单一事实来源（`pyproject.toml`）、`desktop-v*`/`web-v*` tag 规范、两条线可各自演进都不变，只是这一次两个数字恰好相等；下次谁先动谁就先走 | Active | E-036 |
| D-025 | 桌面版对齐 Web 版的**外观与四个头条读数**，不搬趋势图：新增读数卡区（进风温度 / 排风温度 / CPU 温度 / 实时功耗），界面全面中文化，圆角与字号向 `app.css` 的 token 收敛。读数由后台每 60 秒一条 `sdr elist all` 驱动，成功时**不写事件日志**、失败时照写；已有用户命令在执行时直接跳过本轮 | 业主判断趋势图不是桌面版需要的东西，而桌面版也确实没有样本历史（无轮询、无 SQLite），照搬会连带引入一套存储。四个读数只需要一次快照即可，代价小得多。**槽位按传感器名匹配而不是按位置**：不同机箱或拔掉一颗 CPU 都会改变 SDR 里有什么，匹配不到就留 `--`，绝不借用无关传感器凑数——这与 D-023 拒绝显示假 0 W 是同一条原则。轮询定为 60 秒且只发一条命令：完整 SDR 遍历对 iDRAC8 是重操作，反复读会耗尽 BMC 的 IPMI session（D-022 / E-031）；事件日志只记操作员做过的事，后台刷新写进去会把真正的操作淹掉，但失败必须可见，否则链路断了会看起来像“数据有点旧而已” | Active | E-037 |

