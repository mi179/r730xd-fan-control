# EVIDENCE

一条结论只有在这里有行、且指向可核验的文件或命令输出时才算成立。大文件（EXE、镜像包）不进 Git，记录路径 + SHA-256。

| ID | 结论 | 证据 | 日期 |
|---|---|---|---|
| E-001 | 治理骨架引入前，本仓库只有一次提交（`5dfc96e Publish R730xd fan control console`），无增量开发历史 | `git log --oneline`，2026-07-28 实测 | 2026-07-28 |
| E-002 | `.github/workflows/` 目录为空，项目没有任何 CI | `ls .github/workflows` 无输出，2026-07-28 实测 | 2026-07-28 |
| E-003 | 版本号漂移：桌面版 `pyproject.toml` 为 0.4.0，Web 安装器 `webapp/installer/VERSION` 为 0.3.1 | 两文件内容，2026-07-28 实测 | 2026-07-28 |
| E-004 | 截至 2026-07-28 的本地产物哈希：EXE `1BC0903F…F0F3F`（15,222,718 B）；Docker 离线包 `E9758BC5…96BD8D`（23,307,745 B）；镜像包 `F9CD35D5…F0FBAB`（23,376,271 B）。**Superseded by E-030（Web 线）** | `dist/R730xdFanConsole-AllInOne-v0.4.0.exe`、`dist/docker/` 两文件，SHA-256 于 2026-07-28 计算 | 2026-07-28 |
| E-005 | 双形态交付 | `README.md`（GUI 与 `webapp/` 两节）、`webapp/README.md` | 回溯 |
| E-006 | 密码只存内存 | `README.md`“安全设计”第 1 条；`r730xd_fan/ipmi.py` `_validate_settings` 报错文案 | 回溯 |
| E-007 | `-E` 传密码 | `r730xd_fan/ipmi.py` `build_command()`（`"-E"` 入参、`IPMI_PASSWORD` 环境变量） | 回溯 |
| E-008 | 调速联锁与自动/手动 raw 命令 | `r730xd_fan/ipmi.py` `MANUAL_MODE_RAW`/`AUTO_MODE_RAW`、`speed_request()` 5–100 强校验；`README.md`“安全设计” | 回溯 |
| E-009 | 控制区用 iDRAC 凭据、无第二套密码、恒定时间比对 | `webapp/README.md`“快速遥测”；`webapp/app.py` `LoginLimiter`/登录路由 | 回溯 |
| E-010 | Docker secret 方案 | `webapp/README.md`“OpenWrt 启动”与密码章节；`webapp/compose.yaml` secrets 配置 | 回溯 |
| E-011 | MAC 身份与必填启动条件 | `webapp/README.md`“快速遥测”末两条；`webapp/app.py` `MacAddressDiscovery`、`_mac_discovery_from_environment()` | 回溯 |
| E-012 | 防火墙区与固定容器 MAC | `webapp/README.md`“WRT 防火墙”全节（三条窄规则 + `src_mac` 条件说明） | 回溯 |
| E-013 | 不公开内嵌 MSI 的 EXE | `README.md`“一体版自动依赖安装”末段 | 回溯 |
| E-014 | WSL 开发、Windows 运行 | `README.md`“VS Code + WSL 开发”；`scripts/*-from-wsl.sh` 三个脚本 | 回溯 |
| E-015 | LAN HTTP 边界与 HTTPS 反代要求 | `webapp/README.md` 引用块（`WEB_COOKIE_SECURE`） | 回溯 |
| E-016 | 测试存在且分两套：桌面 `tests/`（dependency/ipmi/ui_startup）与 Web `webapp/tests/`（含对部署实例的无损 live smoke） | `tests/test_*.py`、`webapp/tests/` 目录与 `live_smoke.py` 文件头注释 | 2026-07-28 |
| E-017 | CI 建立前仓库存在 21 个 ruff 违规（6 E501 / 6 I001 / 2 F401 等），其中 15 个自动修复、6 个手工修复（含 `main.py` 的 B904 与 `webapp/app.py:851` 的超长行）；修复后桌面 15 + Web 31 测试在 `.venv-win` 下全部通过。推送后首次 CI 运行（run 30328857518）lint / desktop-tests / webapp-tests 三 job 全部 success，证明 windows runner 无头会话可正常创建 Tk 窗口 | `ruff check .` 输出、unittest 运行记录、GitHub Actions API 查询结果，2026-07-28 实测 | 2026-07-28 |
| E-018 | 版本号引用点盘点：桌面版存在于 `pyproject.toml`、`r730xd_fan/__init__.py`、（原硬编码于 `build_windows.ps1`，D-013 后改为从 pyproject 读取）；Web 版单一来源 `webapp/installer/VERSION`，硬编码副本在 `install.sh`、`verify.sh`、`compose.offline.yaml`、`README.txt`、`webapp/compose.yaml`，`build_openwrt_bundle.ps1` 现在对前四个 payload 文件做一致性断言，漂移即拒绝打包。远程仓库原已存在 `web-v0.3.1` tag（指向 5dfc96e），证明 Web 线 tag 命名与 D-013 规范一致 | `grep 0.4.0/0.3.1/VERSION` 全仓扫描、`git ls-remote --tags`，2026-07-28 实测 | 2026-07-28 |
| E-019 | OpenWrt Docker Web v0.3.1 已首次部署到 `192.168.5.2:8088`：容器为 `running/healthy`，独立网络/IP/MAC、Docker secret 权限、Compose、UCI 与 LAN health endpoint 全部通过；实时 nftables 中存在且仅按设计放行 LAN→TCP 8080、容器→TCP 443、容器→UDP 623 三条规则；无损 live smoke 验证匿名监控、iDRAC 凭据登录、受保护配置与退出。安装未发送风扇调速命令。该 Kwrt 的既有 `firewall4` + legacy `miniupnpd-iptables`/失效 `passwall` runtime includes 会使 `fw4 reload` 在 ruleset 已加载后仍返回 1；本次仅在授权窗口临时禁用 `miniupnpd` include，并只在 `fw4 check` 通过且三条实时项目规则全部出现时容忍该返回值，随后恢复 include，原 miniupnpd PID 仍运行，临时 secret/proxy 均已删除 | 离线包 `D:\UserData\Documents\r730xd_fan\dist\docker\R730xdFan-Web-Docker-v0.3.1.tar.gz`，23,307,745 B，SHA-256 `E9758BC562B2AFC55ED18B9F821C068DEB11A9302D0C0E7F29838D896996BD8D`；远端 `sh /opt/r730xd-fan-web/verify.sh` 输出 `VERIFY OK`；本地 `.venv-win\Scripts\python.exe webapp/tests/live_smoke.py` 输出 `LIVE_SMOKE_OK http://192.168.5.2:8088 public-monitor/login/protected-config/logout`；远端 `nft list table inet fw4 \| grep -F Allow-R730xd`，2026-08-02 实测 | 2026-08-02 |

| E-020 | 前端重做落地：`webapp/static/app.css` 从 2468 行（其中前约 1600 行是旧 HTML 结构遗留的死样式，`.blueprint-grid` / `.brand-lockup h1` / `.metric-copy` / `.fan-gauge` / `.connection-block` 在当时的 index.html 中已不存在）重建为约 1070 行，全部规则可从两个模板到达；删除仪表盘 SVG、快照条、sparkline canvas 与对应 JS。浏览器实测：桌面与移动视口 `scrollWidth - clientWidth = 0`（无横向溢出），无 console 错误 | `git show --stat` 对比 `webapp/static/app.css`、`webapp/templates/index.html`、`webapp/static/app.js`；本地假 iDRAC 预览 `http://127.0.0.1:8099` DOM 度量，2026-08-15 实测 | 2026-08-15 |
| E-021 | WRT 宿主存储介质实测：`Kwrt 25.12-SNAPSHOT` / `x86/64`，磁盘为 `VMware Virtual disk`（即虚拟机，非路由器闪存），`/proc/mtd` 为空，overlay 为 f2fs on `/dev/loop0`，803M 总量剩 358M，内存 2 GB，Docker 27.3.1 / Compose v2.40.3，当时无任何 named volume。据此排除 flash 磨损顾虑，D-015 采用落盘方案 | `ssh root@192.168.5.2` 只读执行 `df -h` / `mount` / `cat /proc/mtd` / `block info` / `cat /sys/block/sda/device/model` / `docker volume ls`，2026-08-15 实测 | 2026-08-15 |
| E-022 | SQLite 持久化行为验证：分桶正确（240 条 15 s 样本下 `?range=5m`→20 桶、`1h`→240 桶、`24h`→14 桶）；重复 `observed_at` 走 INSERT OR REPLACE 不累积；保留期裁剪生效；父目录不可写时 `enabled=False` 且不抛异常，`/api/telemetry/history` 继续从内存服务。`?range=` 缺省时行为与改动前完全一致（受 `HISTORY_MAX_SAMPLES` 约束），未知 range 返回 400 `invalid_range`。长区间返回 sqlite 与内存的时间戳并集，避免容器刚重启时趋势反而变稀疏 | `webapp/tests/test_app.py` 的 `TelemetryStoreTests` 与 `TelemetryHistoryRangeTests`（新增 7 个用例）；`webapp` 38 + 桌面 15 测试全通过，`ruff check .` 干净，`scripts/verify_governance.py` OK，2026-08-15 实测 | 2026-08-15 |
| E-023 | 修正一处真实误导：温度卡此前取「最热传感器的状态」作为整卡健康度，当较凉的传感器进入 Warning 时整卡仍显示 OK。改为取该组最差状态（`worstHealth`）。用假 iDRAC 让 `Exhaust Temp` 报 Warning 复现并验证修复，同时验证告警横幅出现、计数为 1、明细行标记 `is-warning` | `webapp/static/app.js` `normalizeSummary()` 温度分支；预览实测 `temperatureHealth` 由 `OK` 变为 `WARNING`，2026-08-15 实测 | 2026-08-15 |

| E-024 | 匿名 SDR 访问与冷却实测：未带任何 cookie 的 `POST /api/sensors/deep-scan` 返回 202 并真实启动扫描，随后 `GET` 返回 `status=complete` 与 57 条记录；冷却期内第二次匿名 POST 返回 200 + `throttled=true`，已登录操作员同一时刻 POST 不带 `throttled`。浏览器访客态（`authenticatedWorkspace` 隐藏、控制门可见）下 SDR 表仍显示全部 57 行，调速区保持锁定 | `curl` 无 cookie 调用记录；`webapp/tests/test_app.py::WebBackendTests::test_anonymous_deep_scan_is_rate_limited_but_operators_are_not`；预览 DOM 度量，2026-08-15 实测 | 2026-08-15 |
| E-025 | 排版与传感器注解：`app.css` 中 `font-family: var(--mono)` 的使用点从覆盖表头/日志/状态条/meta 收敛到 12 处纯数值场景；新增 `SENSOR_GLOSSARY`（28 条正则）为 SDR 名称生成中文注解，实测 57 条记录中 56 条命中（未命中的是构造的无管道符异常行），注解同时进入传感器明细列表、搜索关键字与 CSV 导出。1280×900 与 375×812 两个视口下页面横向溢出均为 0，宽表在自身 `.table-wrap` 内滚动 | `webapp/static/app.js` `SENSOR_GLOSSARY` / `sensorGloss()`；预览实测 `glossCount=56`、`overflow=0`、`tableScrollsInsideWrapper=true`，2026-08-15 实测 | 2026-08-15 |

| E-026 | 证书指纹固定实测（非仅断言参数）：测试起一个真实自签 TLS 服务器，用**错误**指纹的客户端请求时抛 `RequestException` 且服务器端记录到的 `Authorization` 头列表为空（凭据未送达冒充者）；换成**正确**指纹后返回 200 且服务器恰好收到 1 次 Basic 头。指纹解析接受带冒号/大写/裸十六进制，拒绝非 SHA-256 长度与非法字符 | `webapp/tests/test_app.py::RedfishFingerprintPinningTests`（4 个用例，含 `test_pin_mismatch_aborts_before_credentials_are_sent`）；`webapp/app.py` `_FingerprintAdapter` / `_normalise_fingerprint` / `_redfish_client_from_environment`，2026-08-15 实测 | 2026-08-15 |
| E-027 | 子网分桶限速实测：在 `192.168.5.20..26` 七个不同源地址上连续登录失败，前 5 次返回 401、第 6/7 次返回 429（说明换 IP 不再重置预算）；同时从 `192.168.9.5` 发起的失败仍返回 401，证明不同 /24 各自计数 | `webapp/tests/test_app.py::LoginRateKeyTests`（2 个用例）；`webapp/app.py` `_login_rate_key()`，2026-08-15 实测 | 2026-08-15 |
| E-028 | 公开面不变量：遍历 `app.url_map` 取出全部未被 `login_required` 包装的路由（当时 10 个），匿名逐个调用后断言 `FakeIpmi` 收到的调用中不存在 `raw 0x30 0x30 0x01/0x02` 前缀的写命令；另断言 `/api/control/manual|auto|speed` 三条必定在鉴权集合内。Web 路由总数 18（PUBLIC 10 / AUTH 8），公开路由中改变机器状态的为 0 | `webapp/tests/test_app.py::PublicSurfaceInvariantTests`（2 个用例）；Web 48 + 桌面 15 测试全通过，`ruff check .` 干净，2026-08-15 实测 | 2026-08-15 |

| E-029 | 风险接受的复议触发条件（写死，供后续会话判断是否需要重新评估 D-021）：① 该服务以任何形式变得可从内网之外访问（端口转发、VPN 之外的隧道、UPnP、反代对外）；② 内网接入不再是单一信任域（访客 Wi-Fi 与本网段互通、出租/合住、IoT 设备与服务器同段）；③ iDRAC 凭据同时用于其他系统。满足任意一条即须重新评估 T-012 / T-013 的优先级 | 用户 2026-08-15 明确决定「内网可以用就可以上线」，安全加固转待办 | 2026-08-15 |
| E-030 | Web v0.4.0 完整发布链路首次走通：Desktop 15 + Web 51 测试、ruff、治理自检全绿；以提交 `78eeef8` 的 `git archive HEAD:webapp` 为无凭据构建上下文，在 x86_64 WRT 原生构建 image `sha256:fd1db7184f88ba9fb6897ad47b7e180330c6308671c90a2aac1746259979acca`；正式 installer 先按旧容器 image ID 建 `rollback-20260816-001646`，再加载并启动新镜像。既有第三方 fw4 include 仍使 reload 返回非零，E-019/`install.sh` 的严格守卫确认三条项目规则全在 live ruleset 后继续；`verify.sh` 输出 `VERIFY OK`，SQLite 为 UID/GID 10001，`live_smoke.py` 输出 `LIVE_SMOKE_OK`，安装器未发送风扇控制命令 | 镜像归档 `dist/docker/r730xd-fan-web-0.4.0-linux-amd64.tar.gz`，23,497,775 B，SHA-256 `6838FF1AEE873615490C0DA50E1EC0D270432B0EE2F71C6B8520BAFEEF556137`；离线包 `dist/docker/R730xdFan-Web-Docker-v0.4.0.tar.gz`，23,428,725 B，SHA-256 `396369075017D94003D15ABC26B187FA6C9D05030C8F8DC2270D9F0412A6CEFE`；远端 image inspect / `verify.sh` / nft / stat 与本地 live smoke 输出，2026-08-16 实测 | 2026-08-16 |
| E-031 | iDRAC8 完整 SDR 的真实兼容边界：WRT 镜像内 `ipmitool 1.8.19` 对 firmware 2.70 执行 `sdr elist all` 会段错误，崩溃前能输出真实记录，反复运行会使后续命令出现 `insufficient resources for session`。D-022 修复后只接受 `SIGSEGV` 返回码 `-11/139` 且至少一条有效记录；认证等其他非零退出和空输出仍失败。部署后只触发一次真机扫描，2.691 s 返回 83 条、`status=complete`、`partial=true`、`partial_reason=ipmitool_sigsegv`；常规 Redfish 遥测和风扇控制边界不受影响。浏览器实测新版页面 iDRAC ONLINE、无横向溢出、无 console error，线上 `app.js` 含 partial 横幅与 WARN 文案 | `webapp/app.py` `Backend._run_deep_scan()`；`webapp/tests/test_app.py` 三个 `test_deep_scan_*sigsegv/nonzero*` 用例；线上 `GET /api/sensors/deep-scan` 与 Browser 检查，2026-08-16 实测。当前 MAC `d0:94:66:8c:e0:e3` 在 WRT ARP 表对应 `192.168.5.130`，证明 STATUS 原 `.151` 已过期 | 2026-08-16 |

| E-032 | iDRAC8 firmware 2.70 无效 `PowerMetrics` 实证与修复验证：真机只读 Redfish 返回 `PowerConsumedWatts=135` 同时 `PowerMetrics` 的 Average/Min/Max 全为 0；同期 `GET /api/telemetry/history?range=5m` 有 15 个真实功耗样本（131–138 W，均值 133.8 W），证明数据源本身正常。修复后在复现该缺陷的本地假 iDRAC 上实测：公开 telemetry 的 power 只剩 `consumed/allocated/capacity` 三个字段，三个 metrics 字段不再出现；功耗页显示 190.3 / 166.5 / 204 W 并标注「按当前趋势区间（5 分钟）的 22 个图表样本计算 · iDRAC 未提供有效数值」；完整扫描分类下拉显示每类计数（全部 52 / 温度 28 / 风扇 7 / 功耗 0 / 电压 0），选「功耗」时空行显示「当前部分结果未包含功耗记录 —— ipmitool 在遍历完 SDR 前退出」，浏览器无 console error | `webapp/app.py` `_parse_redfish_telemetry()` 的 impossible-metrics 判定与 `_sample_statistics()`；`webapp/tests/test_app.py` 的 `ImpossiblePowerMetricsTests`、`SampleStatisticsTests` 共 7 个用例；Web 58 + 桌面 15 测试通过，`ruff check .` 干净，2026-08-16 实测 | 2026-08-16 |

| E-033 | Web v0.4.1 构建、部署与验收：镜像在 WRT 上原生构建（x86_64，无跨架构），`docker save` 回传后经 `build_openwrt_bundle.ps1` 打包并由 `install.sh --non-interactive` 安装。安装期间 `firewall reload` 仍因该 Kwrt 既有失效 include 返回非零，E-019 的守卫按设计生效（`fw4 check` 通过 + 三条项目规则实时存在 → 继续）。验收：`verify.sh` 输出 `VERIFY OK` 且 `image=r730xd-fan-web:0.4.1 state=running health=healthy`；三条 nftables 规则存在；`/data/telemetry.db` 属主 `r730xd`(10001)；`live_smoke.py` 输出 `LIVE_SMOKE_OK`；安装器未发送任何 fan-control command。**D-023 修复真机确认**：公开 telemetry 的 power 只剩 `consumed/allocated/capacity`，三个无效 metrics 字段不再下发；功耗页显示 实时 133 W、平均 131.6 / 最小 129 / 最大 134 W（修复前为 0 W），并标注「按当前趋势区间（5 分钟）的 11 个图表样本计算 · iDRAC 未提供有效数值」；页面横向溢出 0，告警横幅在无异常时不占位 | 产物（不进 Git）：`dist/docker/r730xd-fan-web-0.4.1-linux-amd64.tar.gz` 23,499,519 B SHA-256 `4E57C19490BA0C905F692B3A2ACE4FAB68742F591B929FF12151BC665629A4D8`；`dist/docker/R730xdFan-Web-Docker-v0.4.1.tar.gz` 23,429,361 B SHA-256 `7238EBC90107994F785942F2B04592294B8342F5450DCA8A6354BBFCE8CE79D0`；运行中容器镜像 `sha256:fb2e322343eacb92a4d925a9dd517b7e9685e118b3f0de1d11964990a0478e20`，2026-08-16 实测 | 2026-08-16 |
| E-034 | ~~部署副产物累积实测：`install.sh` 每次部署新增 rollback 标签导致 overlay 可用从 358 MB 降到 276 MB~~ **本行的 overlay 归因错误，见 E-035 更正**。`/tmp` 残留部分成立：8 个构建/部署目录与归档约 205 MB，且 `/tmp` 是 tmpfs，占的是内存 | 远端只读 `du -sh /tmp/r730xd-*`、`df -h /tmp /overlay`、`docker images`，2026-08-16 实测 | 2026-08-16 |
| E-035 | **更正 E-034 的 overlay 归因。** 删除 5 个 `rollback-*`/`candidate-*` 标签后 overlay 可用仅从 276.4 MB 变为 276.9 MB —— 这些是指向同一批镜像的**标签**而非独立镜像，解标签不释放空间（去重后 `r730xd-fan-web` 只有 3 个不同 image ID：0.4.1 `fb2e322343ea`、0.4.0 `fd1db7184f88`、0.3.1 `855ebc308bad`）。`docker system df` 实测 Images 6 个共 221.1 MB，其中 **130.7 MB（59%）reclaimable**，Build Cache 0 B。overlay 从 358 MB 降到 276 MB 的真实原因是**在 WRT 上构建镜像会拉取 `python:3.12-alpine` 基础镜像并保留其层** —— 这是本项目选定的「在目标机原生构建」路径的固有代价（v0.3.1 时代镜像是 `docker load` 进来的，没有这部分）。因此 T-017 方向由「标签保留策略」更正为「旧镜像与基础镜像回收策略」。`/tmp` 清理实测有效：205.8 MB → 2.9 MB，释放约 203 MB 内存；清理后 `verify.sh` 仍 `VERIFY OK`、`live_smoke.py` 仍 `LIVE_SMOKE_OK`，运行容器未受影响 | 远端只读 `docker system df`、`docker images --format '{{.Tag}} {{.ID}}'`、`df -h /tmp /overlay` 清理前后对比，2026-08-16 实测 | 2026-08-16 |

## 外部原始资料（不进仓库）

| 资料 | 位置 | 说明 |
|---|---|---|
| Dell R730xd 官方手册 PDF | `D:\UserData\Documents\r730xd-ompublication-zh-cn.pdf` | 厂商原始文档，摘要不能替代原文 |
| E-036 | **桌面产物与源码漂移**：`dist/R730xdFanConsole-AllInOne-v0.4.0.exe` 构建于 2026-07-14 17:17，而 `r730xd_fan/ui.py` 最后改动是 2026-08-15 的 `6a39293`（D-014 近黑单色重做）；`pyproject.toml` 的 `version` 自首次提交 `5dfc96e` 起恒为 0.4.0，从未 bump。即产物里没有新 UI，版本号却与源码完全相同，外部无法分辨。按 D-024 重建：**新产物 `dist/R730xdFanConsole-AllInOne-v0.4.1.exe` 21,741,984 B SHA-256 `9B01A3C11B1AE9929651C2E3A3C17CB779FA96A7453E8F3A181C4B9453959169`（**该哈希已被 E-037 取代**：同一天又做了 D-025 的 UI 改动，v0.4.1 从未发布，故不升版号、直接重建）**（旧 v0.4.0 为 15,222,718 B，见 E-004）。构建前置：15 个桌面 unittest OK、`ruff check .` 通过；`build_windows.ps1` 已校验 `C:\OpenManage\BMC.msi` 的 SHA-256 与 Authenticode 签名有效。启动烟测：进程存活超过 10 s 未自行退出，随后主动结束（功能验收仍需人工双击操作一遍） | `git log -1 -- r730xd_fan/ui.py`、`git log -p -- pyproject.toml`、`dist/` 文件时间戳、`build_windows.ps1` 输出 `BUILD_OK ... 21741984 bytes`、`Get-FileHash`，2026-08-19 实测 | 2026-08-19 |
| E-037 | D-025 落地并重建 v0.4.1 一体 EXE。UI 侧实测（`ImageGrab` 截图核对，假数据、未连接任何 BMC）：读数卡显示 进风 23 °C / 排风 35 °C / CPU 48 °C / 实时功耗 133 W，各带传感器原名与「正常」徽章；界面无英文残留（脚本核对 `text=` 字面量，全大写英文串 0 个）；**D-017 违规 0 个**（脚本按控件构造块核对，任何含 CJK 的 `text=` 都不再落在 `Cascadia Mono` 上，仪表盘大数字仍是等宽）。布局回归并修复：读数卡吃掉约 150 px 后 `02 / 连接` 整块掉到折叠线以下，默认窗口 1120x820 → 1180x940 且事件日志 170 → 128 px，连接设置重新在首屏可见。测试：桌面 22 项（新增 3 项 UI 行为 + 4 项槽位选取）、Web 58 项、`ruff` 与治理自检全通过。**产物 `dist/R730xdFanConsole-AllInOne-v0.4.1.exe` 21,748,377 B SHA-256 `5310F0C02CB733B5C389C1E77C8CC69317869AE10AA558B1927D2038FC6A051E`**，启动烟测进程存活超过 12 s 后主动结束 | `tests/test_ui_startup.py`、`tests/test_ipmi.py::KeyReadingTests`、`build_windows.ps1` 输出 `BUILD_OK ... 21748377 bytes`、`Get-FileHash`，2026-08-19 实测 | 2026-08-19 |
| E-038 | D-026 的量化依据与静态验收。**体积**：`deploy_wrt.ps1 -StageOnly` 实测源码包 **65.9 KB**，对比离线包 23,429,361 B（E-033）——小约 356 倍，且只单向过一次网，镜像完全不动。包内清单核对无 `secrets/`、无 `.env`、无 `tests/`，`build/` 恰好是 Dockerfile 会 `COPY` 的集合（`requirements.txt` / `app.py` / `templates` / `static` + `Dockerfile` / `.dockerignore`）。**静态验收**：`sh -n install.sh` 通过；`sh install.sh --help` 列出 `--reconfigure` 与 `--use-local-image`；未知选项仍 `exit 1`；在非 OpenWrt 环境下 preflight 照常拒绝（`uci is required`）并触发失败处理。三个 `.ps1` 全部 `Parser::ParseFile` 无错。**踩到的坑**：`tar.exe -czf D:\...` 在 MSYS/Git-Bash 的 GNU tar 下会把 `D:` 当成 `host:path` 远程规格而失败（System32 的 bsdtar 无此问题），改为 `Push-Location` + 相对路径。**未验证**：真机部署一轮（构建 / 安装 / 自动 verify / 回滚路径）尚未在 `192.168.5.2` 上跑过，需业主授权 | `scripts\deploy_wrt.ps1 -StageOnly` 输出、`sh -n`、`sh install.sh --help`、`[System.Management.Automation.Language.Parser]::ParseFile`，2026-08-19 实测 | 2026-08-19 |
| E-039 | D-027 落地实测。**重复消除**：`webapp/app.py` 中 `grep '"0x30"'` 由 3 处（两个常量 + 第 2111 行调速行内字面量）降为 0；`_sensor_category` / `_parse_sdr_records` / `_extract_reading_number` / `_redact_and_limit` 四个函数体删除，改为绑定到 core。桌面侧 `SensorReading` 成为 `SdrRecord` 别名，`parse_sensor_output` 只剩一行委托。**跨线一致性**由 `tests/test_core_protocol.py` 10 项断言锁定，其中一项直接比较 `webapp.MANUAL_MODE_RAW is protocol.MANUAL_MODE_ARGS`，另一项驱动两端各自的解析器比较分类结果。**打包**：`pyproject.toml` 的 `packages.find` 原为 `["r730xd_fan*"]`，不加 `r730xd_core*` 则 `pip install -e .` 静默漏装、CI 立刻 ImportError。**Docker 上下文**：`Dockerfile` 增 `COPY r730xd_core`，`deploy_wrt.ps1` 把仓库根的 `r730xd_core/` 复制进 `build/` 并顺手剔除 `__pycache__`；`-StageOnly` 实测源码包 68.9 KB，清单含 4 个 core 文件、无 `secrets/` / `.env` / 字节码。**界面**：截图与重构前比对，差异仅剩两处时间戳（读数条与事件日志的时钟）。**产物**：`dist/R730xdFanConsole-AllInOne-v0.4.1.exe` 21,764,046 B SHA-256 `789F191E176C12A7E918608051C5390E4DFEE4EB4FB36A710F0059BCC90521E2`（取代 E-037 的哈希），PyInstaller 能跟到新顶层包，启动烟测存活 >12 s。**测试**：桌面 69 项、Web 58 项、ruff、治理自检全绿；Linux 下 69 项跑 58 项（11 项 GUI 跳过） | `grep`、`tests/test_core_protocol.py`、`deploy_wrt.ps1 -StageOnly`、`build_windows.ps1`、`Get-FileHash`、截图逐像素比对，2026-08-19 实测 | 2026-08-19 |
| E-040 | D-028 真机验证（业主本机 `192.168.5.152`，目标为其自有 iDRAC）。**零知识发现**：`probe_rmcp("192.168.5.0/24")` 在不提供任何 MAC 或地址的前提下返回 `['192.168.5.130']`，即网段内唯一应答 IPMI presence ping 的设备；再读 ARP 得到 `d0:94:66:8c:e0:e3`。**反向定位**：`address_for_mac` 由该 MAC 得回 `192.168.5.130`，大小写与横杠写法均可。**真实按钮**：驱动 `ConnectionDialog._scan()` 实跑一次，地址栏自动填入 `192.168.5.130`、`FanConsole.remembered_mac` 记为该 MAC，界面提示"找到 192.168.5.130 (d0:94:66:8c:e0:e3)"。**布局回归并修复**：新增的扫描按钮与提示行被对话框原 `580x520` 裁掉一半，改为 `580x620`。**根因确认**：桌面版此前写死 `config.py:8` 的 `DEFAULT_HOST = "192.168.5.151"`，实测该地址 ping 不通（E-031 已记录其过期），这就是业主打开 exe 超时的原因。**测试**：`tests/test_discovery.py` 13 项覆盖两种 ARP 版式互证、广播与未解析条目剔除、tag 不匹配/端口不对/包截断的应答一律拒绝、/16 直接拒扫。桌面 82 项、Web 58 项、ruff、治理自检全绿；Linux 下 82 项跑 71 项 | `r730xd_core/discovery.py`；scratchpad 的 `preview_dialog.py` 实跑截图；`Test-Connection 192.168.5.151` 失败 / `.130` 成功，2026-08-19 实测 | 2026-08-19 |
| E-041 | D-029 **真实构建验证**（WSL 内的 Docker，非静态推断）。`docker build -f webapp/Dockerfile -t r730xd-fan-web:ctx-test .` 从仓库根成功。镜像内 `/app` 恰为 `app.py` / `r730xd_core` / `requirements.txt` / `static` / `templates`，`/app/r730xd_core` 含全部 5 个模块；容器内 `python -c "import app; from r730xd_core import protocol"` 通过，返回 `('raw', '0x30', '0x30', '0x01', '0x00')`。**密码未泄漏**：镜像内 `find / -xdev -name idrac_password -o -name .env -o -name test_app.py` **零结果**。`deploy_wrt.ps1 -StageOnly` 的暂存布局改为镜像仓库结构（`build/webapp/…` + `build/r730xd_core/`），实测 71.7 KB。**非 OpenWrt 部署的关键坑已定位并写入文档**：`webapp/app.py:339` 要求 ARP 行的网卡名等于 `IDRAC_ARP_INTERFACE`，其默认值 `br-lan` 为 OpenWrt 专有，普通 Linux 上会过滤掉全部 ARP 记录、导致自动发现**静默失效且不报错**；逃生口是置空。验证后已删除测试镜像 | `docker build` / `docker run --entrypoint sh` 实测、`deploy_wrt.ps1 -StageOnly`、`grep webapp/app.py`，2026-08-19 实测 | 2026-08-19 |
| E-042 | D-030 实测。**DPI 缩放缺陷确认**：探针显示 `geometry("1180x940")` 后 `winfo_width()` 返回 1770（本机 150% 缩放），故物理/逻辑混比会让断点永不触发——修复前三档尺寸全部落在 `(4, True, False)`；换算为逻辑像素后依次得到 `(4, True, False)` / `(2, True, False)` / `(2, False, True)`。**minsize 阻挡确认**：降到 560x520 之前，请求 620x700 实际只能得到 900x700（逻辑），堆叠档永不可达。**无抖动**：`_apply_layout` 在每档只被调用 1 次，40 轮 `update()` 内不再触发，说明 `_layout_key` 守卫有效、重排不会自激。**回归测试**：`tests/test_ui_startup.py` 新增两项，断言各档下读数卡的 row/column、左右两栏在窄档处于不同 row（堆叠）、短窗时 `log` 未映射而 `log_summary` 已映射，以及**折回宽档能恢复**。顺带把仪表盘接到此前建好却未使用的 `presenters.gauge_tone`，并给极短弧设 1.5° 下限（低转速时 14° 粗弧看起来像一个脱离的方块）。**本轮未取得截图**：`ImageGrab` 在本机持续报 `screen grab failed`，故改以结构断言验证——对"折叠"这一属性而言这反而是更强的证据，可重复且能防回归。**产物**：`dist/R730xdFanConsole-AllInOne-v0.4.1.exe` 21,782,343 B SHA-256 `84852E52C29FD2561B99498EECE25A93E72F11B176FD61582A8FF97A9B586B50`（取代 E-039），烟测存活 >12 s。桌面 84 项、Web 58 项、ruff、治理自检全绿；Linux 下 84 项跑 71 项 | scratchpad 的 `probe_resize.py` 输出、`tests/test_ui_startup.py` 布局用例、`build_windows.ps1`、`Get-FileHash`，2026-08-19 实测 | 2026-08-19 |
| E-043 | D-031 双平台实测。**Windows**：`host_addresses()` 读出 7 个接口，本机同时挂在 `192.168.5.152/24` 与 `192.168.1.11/24` 两个 LAN 上（另有 WSL 的 `172.20.128.1/20` 和多个 169.254 链路本地地址）；`scan_range()` 以 UDP 路由选出的 `192.168.5.152` 为准，得 `192.168.5.0/24`、`assumed=False`（掩码是读出来的不是猜的）。**Linux**：WSL 的 `eth0` 真实为 `172.20.137.77/20`，超过上限，收窄为 `172.20.137.0/24` 且 `narrowed=True`、note 明确写出"本机网段是 /20，太大，只扫本机所在的 /24"。**真机复跑**：改动后扫描仍返回 `192.168.5.130 / d0:94:66:8c:e0:e3`。**上限修正**：原 256 会把 /23（512 地址）判为超限而收窄，测试首次运行即暴露此点——/23 是常见小办公网段，收窄可能把 BMC 漏在未扫的一半，故上限提至 512 并由 `MAX_SCAN_HOSTS` 统一给 `probe_rmcp` / `discover` / `scan_range` 三处默认值。**顺带简化**：对话框此前在探测**之前**读一次 ARP（那时必然是旧的）再在之后补读一次；现已删除前一次，因为探测本身才是 ARP 条目的成因。测试 +8（真实掩码、多网卡选路、/23 完整扫、/20 收窄、读不到掩码时回落并告知、收窄结果必须仍在上限内、以及对真实操作系统跑一次接口解析）。桌面 92 项、Web 58 项、ruff、治理自检全绿；Linux 下 92 项跑 79 项 | `host_addresses()` / `scan_range()` 双平台输出、`discovery.discover` 真机复跑、`tests/test_discovery.py`，2026-08-19 实测 | 2026-08-19 |
